package com.resume.parser.parser.controller;

import java.io.BufferedReader;
import java.io.Reader;
import java.io.StringReader;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Dummy {

	public  static void main(String[] args) {
	/*ArrayList<Reader> list=new ArrayList<Reader>();
	
		String txt = "NARASIMMARAJA BS \r\n"
				+ "Email :narasimmaraja.bs@gmail.com               \r\n"
				+ "Phone:8838206305      \r\n"
				+ "LinkedIn: https://www.linkedin.com/in/narasimmaraja-bs-364489236/ \r\n"
				+ "Github : https://github.com/bs-narasimmaraja \r\n"
				+ " EDUCATION & TRAINING: Full Stack Developer July2023-Mar202 Jspiders , BengaluruJava Full Stack DevelopmentBachelor of engineering (Mechanical Engineering) Aug2020-May2023 Mountzion college of Engineering and technology Secured 8.25 CGPADiploma (Mechanical Engineering) Aug2017-May2020Annamalai Polytechnic College Secured 73%";
		
		Reader reader=new StringReader(txt);
		if(reader.equals("Eduactional && Training")) {
			while(!reader.equals("Skills")) {
				list.add(reader);
			}
		Iterator iter=list.iterator();
		
		while(iter.hasNext()) {
			System.out.println(iter.next());
		}*/
		
//		String Regex="\\b([A-Z][a-z]+(?:\\s[A-Z][a-z]+)*)\\b";
			//String Regex= "\\S+@\\S+";
		// 	String word = "raja\b\\s\\s\snnnn";
			//String Regex="\\d{10,13}";
		
		/*
		 * Pattern pattern =Pattern.compile(Regex); Matcher
		 * matcher=pattern.matcher(txt);
		 * 
		 * while(matcher.find()) { list.add(matcher.group(0)); }
		 * System.out.println(list.get(0)); ArrayList word=new ArrayList();
		 * while(list.get(0)!=null||list.get(0)!="Email") { word.add(list.get(0)); }
		 * System.out.println(word.get(0));
		 */
         
    
		/*String email = list.get(0);
		if(!Character.isDigit(email.charAt(0))||!Character.isLetter(email.charAt(0))) {
			email=email.substring(1);
		}
		
		System.out.println(email);;*/
	//}
	
	
	/*
	 * List<String> words = new ArrayList<>(); words=extractWords(pdfText);
	 * if(words.get(2)!=null&&words.get(2)!="phone") {
	 * //resume.setName(words.get(0));
	 * 
	 * resume.setEmail(words.get(2)); } //
	 * resume.setName(extractName(pdfText).get(0));
	 * System.out.println(resume.getEmail());
	 * 
	 * 
	 * 
	 * return "parsed Successfully"; }
	 * 
	 * public ArrayList<String> extractWords(String Pdftext) { ArrayList<String>
	 * list=new ArrayList<String>(); //String
	 * Regex="\\b([A-Z][a-z]+(?:\\s[A-Z][a-z]+)*)\\b"; //String Regex="\\w*\\S\\n*";
	 * String Regex= "\\w*\\n*\\D"; Pattern pattern =Pattern.compile(Regex); Matcher
	 * matcher=pattern.matcher(Pdftext);
	 * 
	 * while(matcher.find()) { list.add(matcher.group(0));
	 * 
	 * } return list; }
	 */

	}}

