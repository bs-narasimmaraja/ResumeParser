package com.resume.parser.parser.model;

public class Testt {
	/*
	 * public static void main(String[] args) { String s1="932222222";
	 * validateMDN(s1); } public static String validateMDN(String mdn) { try { if
	 * (mdn == null || mdn.equals("") || mdn.equals("null") || mdn.length() < 10) {
	 * mdn = "INVALIDMDN"; } else { mdn = extractDigits(mdn.trim()); if
	 * (mdn.length() >= 10) { mdn = mdn.substring(mdn.length() - 10); } } } catch
	 * (Exception e) { logger.info("validateMDN :: Exception occurred: " + e); mdn =
	 * "INVALIDMDN"; } finally { logger.info("validateMDN :: Final MDN: " + mdn); }
	 * return mdn; }
	 * 
	 * public static String extractDigits(String data) { String digits = ""; for
	 * (int j = 0; data != null && j < data.length(); j++) { int c = data.charAt(j);
	 * if (c > 47 && c < 58) { // ASCII check for digits 0-9 digits = digits +
	 * data.charAt(j); } } if (digits.equals("")) { digits = "Error"; } return
	 * digits; }
	 */}
